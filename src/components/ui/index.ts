import { Label } from "./label";
import { Button } from "./button";
import { Input } from "./input";
import { Spinner } from "./spinner";

export const ui = {
  Label,
  Button,
  Input,
  Spinner,
};
